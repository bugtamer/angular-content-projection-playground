import { InjectorDirective } from './injector.directive';

describe('InjectorDirective', () => {
  it('should create an instance', () => {
    const directive = new InjectorDirective();
    expect(directive).toBeTruthy();
  });
});
